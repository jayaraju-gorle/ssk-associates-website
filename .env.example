# --- AI assistant (required for the chat widget) ---
# Get a key at https://platform.claude.com/
ANTHROPIC_API_KEY=sk-ant-...

# --- Lead email delivery (optional — leads are always saved to leads.json too) ---
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_USER=your Gmail,
# SMTP_PASS=an App Password (Google Account > Security > 2-Step Verification > App passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=Sasumana.saikumar@gmail.com
SMTP_PASS=your-app-password

# Where enquiries are sent
LEAD_TO_EMAIL=Sasumana.saikumar@gmail.com

PORT=3000
